<div class="comments add-margin">
    {{#each this}}
    <section class="comment">
        <div class="meta bold">
            <h5>{{user.username}} on {{format_date created_at}}</h5>
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>